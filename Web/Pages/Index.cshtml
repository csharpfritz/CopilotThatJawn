@page
@model IndexModel
@{
    ViewData["Title"] = "Copilot That Jawn - AI Tools & Tips";
}

<!-- Hero Section -->
<div class="hero-section bg-primary bg-gradient text-white py-5 mb-5">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-7 col-md-8">
                <h1 class="display-3 fw-bold mb-3">Copilot That Jawn</h1>
                <p class="lead fs-4 mb-4">Your comprehensive hub for Microsoft Copilot, GitHub Copilot, and AI productivity tools. Discover tips, tricks, and resources to supercharge your workflow.</p>
                <div class="d-flex flex-wrap gap-2">
                    <a asp-page="/Tips/Index" class="btn btn-light btn-lg">Browse All Tips</a>
                    <a asp-page="/Tips/Category" asp-route-category="github-copilot" class="btn btn-outline-light btn-lg">GitHub Copilot</a>
                </div>
            </div>
            <div class="col-lg-5 col-md-4 text-center">
                <!-- Placeholder for an illustration or logo -->
                <i class="bi bi-robot display-1"></i>
            </div>
        </div>
    </div>
</div>

<!-- Quick Links Section -->
<div class="container mb-5">
    <div class="row g-4">
        <div class="col-lg-3 col-md-6">
            <div class="card text-bg-success h-100 border-0 shadow-sm hover-card">
                <div class="card-body text-center p-4">
                    <i class="bi bi-github display-5 mb-3"></i>
                    <h4 class="card-title">GitHub Copilot</h4>
                    <p class="card-text mb-4">AI pair programming with GitHub Copilot.</p>
                    <a href="#" class="btn btn-outline-light stretched-link">Explore</a>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6">
            <div class="card text-bg-primary h-100 border-0 shadow-sm hover-card">
                <div class="card-body text-center p-4">
                    <i class="bi bi-microsoft display-5 mb-3"></i>
                    <h4 class="card-title">Microsoft 365</h4>
                    <p class="card-text mb-4">Productivity with Microsoft 365 Copilot.</p>
                    <a href="#" class="btn btn-outline-light stretched-link">Explore</a>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6">
            <div class="card text-bg-info h-100 border-0 shadow-sm hover-card">
                <div class="card-body text-center p-4">
                    <i class="bi bi-cloud display-5 mb-3"></i>
                    <h4 class="card-title">Azure AI</h4>
                    <p class="card-text mb-4">Azure AI services and integration guides.</p>
                    <a href="#" class="btn btn-outline-light stretched-link">Explore</a>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6">
            <div class="card text-bg-warning h-100 border-0 shadow-sm hover-card">
                <div class="card-body text-center p-4">
                    <i class="bi bi-tools display-5 mb-3"></i>
                    <h4 class="card-title">AI Tools</h4>
                    <p class="card-text mb-4">Complementary AI tools and resources.</p>
                    <a href="#" class="btn btn-outline-light stretched-link">Explore</a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Tips Section -->
<div class="container mb-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold"><i class="bi bi-lightning-charge"></i> Recent Tips</h2>
        <a asp-page="/Tips/Index" class="btn btn-outline-primary">View All Tips</a>
    </div>
    
    <div class="row g-4">
        @foreach (var tip in Model.RecentTips)
        {
            <div class="col-lg-4 col-md-6">
                <div class="card h-100 shadow-sm hover-card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between mb-2">
                            <span class="badge bg-primary rounded-pill">@tip.Category</span>
                            <small class="text-muted">@tip.PublishedDate.ToString("MMM dd, yyyy")</small>
                        </div>
                        <h5 class="card-title">@tip.Title</h5>
                        <p class="card-text text-truncate-3">@tip.Description</p>
                        <div class="d-flex gap-1 mb-2">
                            @foreach (var tag in tip.Tags.Take(3))
                            {
                                <span class="badge bg-light text-dark">#@tag</span>
                            }
                            @if (tip.Tags.Count > 3)
                            {
                                <span class="badge bg-light text-dark">+@(tip.Tags.Count - 3)</span>
                            }
                        </div>
                    </div>
                    <div class="card-footer bg-white border-0">
                        <div class="d-flex justify-content-between align-items-center">
                            <small class="text-muted"><i class="bi bi-clock"></i> @tip.ReadingTimeMinutes min read</small>
                            <a asp-page="/Tips/Details" asp-route-slug="@tip.UrlSlug" class="btn btn-sm btn-primary">Read More</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<!-- Categories Section -->
<div class="container-fluid bg-light py-5 mb-5">
    <div class="container">
        <h2 class="fw-bold mb-4"><i class="bi bi-folder2-open"></i> Explore Categories</h2>
        <div class="row g-4">
            @foreach (var category in Model.Categories)
            {
                <div class="col-lg-3 col-md-4 col-sm-6">
                    <a asp-page="/Tips/Category" asp-route-category="@category.ToLowerInvariant()" 
                       class="card text-decoration-none text-dark hover-card h-100">
                        <div class="card-body d-flex align-items-center gap-3">
                            <i class="bi bi-folder2 fs-3"></i>
                            <h5 class="card-title mb-0">@category</h5>
                        </div>
                    </a>
                </div>
            }
        </div>
    </div>
</div>

<!-- Stats Counter Section -->
<div class="container-fluid bg-light py-5 mb-5">
    <div class="container">
        <div class="row g-4 text-center">
            <div class="col-lg-3 col-md-6">
                <div class="stats-counter mb-3">
                    <div class="display-3 fw-bold text-primary counter" data-target="@Model.TotalTipsCount">0</div>
                </div>
                <h5>AI Tips & Tricks</h5>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="stats-counter mb-3">
                    <div class="display-3 fw-bold philly-eagles-green counter" data-target="@Model.Categories.Count">0</div>
                </div>
                <h5>Categories</h5>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="stats-counter mb-3">
                    <div class="display-3 fw-bold philly-phillies-red counter" data-target="@Model.PopularTags.Count">0</div>
                </div>
                <h5>Topics</h5>
            </div>
            <div class="col-lg-3 col-md-6">
                <div class="stats-counter mb-3">
                    <div class="display-3 fw-bold text-info counter" data-target="5000">0</div>
                </div>
                <h5>Monthly Visitors</h5>
            </div>
        </div>
    </div>
</div>

<!-- Popular Tags Section -->
<div class="container mb-5">
    <h2 class="fw-bold mb-4"><i class="bi bi-tags"></i> Popular Tags</h2>
    <div class="d-flex flex-wrap gap-2">
        @foreach (var tag in Model.PopularTags)
        {
            <a asp-page="/Tips/Tag" asp-route-tag="@tag.ToLowerInvariant()" 
               class="btn btn-outline-secondary btn-lg mb-2">
                #@tag
            </a>
        }
    </div>
</div>

<!-- Add JavaScript for the counter animation -->
<script>
    document.addEventListener("DOMContentLoaded", function() {
        const counters = document.querySelectorAll('.counter');
        const speed = 200;
        
        counters.forEach(counter => {
            const updateCount = () => {
                const target = +counter.getAttribute('data-target');
                const count = +counter.innerText;
                
                const increment = target / speed;
                
                if (count < target) {
                    counter.innerText = Math.ceil(count + increment);
                    setTimeout(updateCount, 1);
                } else {
                    counter.innerText = target;
                }
            };
            
            updateCount();
        });
    });
</script>

<!-- Featured Tutorial Section -->
<div class="container mb-5">
    <div class="row">
        <div class="col-lg-8 mb-4 mb-lg-0">
            <div class="card shadow border-0 overflow-hidden">
                <div class="card-header bg-dark text-white">
                    <h3 class="mb-0"><i class="bi bi-mortarboard"></i> Featured Tutorial</h3>
                </div>
                <div class="card-body p-0">
                    <div class="row g-0">
                        <div class="col-md-5">
                            <div class="bg-light h-100 d-flex align-items-center justify-content-center p-4">
                                @if (Model.FeaturedTip != null && Model.FeaturedTip.Category.Contains("GitHub"))
                                {
                                    <i class="bi bi-github philly-eagles-green" style="font-size: 6rem;"></i>
                                }
                                else if (Model.FeaturedTip != null && Model.FeaturedTip.Category.Contains("Microsoft 365"))
                                {
                                    <i class="bi bi-microsoft philly-phillies-red" style="font-size: 6rem;"></i>
                                }
                                else if (Model.FeaturedTip != null && Model.FeaturedTip.Category.Contains("Azure"))
                                {
                                    <i class="bi bi-cloud text-info" style="font-size: 6rem;"></i>
                                }
                                else
                                {
                                    <i class="bi bi-robot text-primary" style="font-size: 6rem;"></i>
                                }
                            </div>
                        </div>
                        <div class="col-md-7 p-4">
                            @if (Model.FeaturedTip != null)
                            {
                                <h4>@Model.FeaturedTip.Title</h4>
                                <p class="text-muted mb-3">@Model.FeaturedTip.Description</p>
                                <div class="d-flex align-items-center mb-3">
                                    <i class="bi bi-clock me-2"></i>
                                    <span>@Model.FeaturedTip.ReadingTimeMinutes min read</span>
                                    <span class="mx-2">•</span>
                                    <i class="bi bi-calendar me-2"></i>
                                    <span>@Model.FeaturedTip.PublishedDate.ToString("MMM dd, yyyy")</span>
                                </div>
                                <a asp-page="/Tips/Details" asp-route-slug="@Model.FeaturedTip.UrlSlug" class="btn btn-primary">Read Tutorial</a>
                            }
                            else
                            {
                                <h4>Getting Started with GitHub Copilot</h4>
                                <p class="text-muted mb-3">Learn how to set up and use GitHub Copilot effectively in your development workflow.</p>
                                <div class="d-flex align-items-center mb-3">
                                    <i class="bi bi-clock me-2"></i>
                                    <span>15 min read</span>
                                </div>
                                <a href="#" class="btn btn-primary">Read Tutorial</a>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>        <div class="col-lg-4">
            <div>
                @await Component.InvokeAsync("NewsletterSignup")
            </div>
            <div class="mt-4">
                <h4 class="mb-3"><i class="bi bi-lightning"></i> Random Tip</h4>
                @await Component.InvokeAsync("RandomTip")
            </div>
        </div>
    </div>
</div>

<!-- Popular AI Tools Section -->
@await Component.InvokeAsync("PopularTools")

<!-- Site Sections -->
<div class="container mb-5">
    <h2 class="fw-bold mb-4"><i class="bi bi-grid-3x3-gap"></i> Discover More</h2>
    <div class="row g-4">
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm overflow-hidden h-100">
                <div class="row g-0 h-100">
                    <div class="col-md-5 bg-primary d-flex align-items-center justify-content-center text-white">
                        <div class="p-4 text-center">
                            <i class="bi bi-journals display-1 mb-3"></i>
                            <h3>Tutorials</h3>
                        </div>
                    </div>
                    <div class="col-md-7">
                        <div class="card-body h-100 d-flex flex-column">
                            <h4 class="card-title">Step-by-Step Guides</h4>
                            <p class="card-text">Comprehensive tutorials that take you through every step of using AI tools to solve real problems.</p>
                            <ul class="list-unstyled mt-auto">
                                <li class="mb-2"><i class="bi bi-check2 text-success me-2"></i> Detailed setup instructions</li>
                                <li class="mb-2"><i class="bi bi-check2 text-success me-2"></i> Practical examples</li>
                                <li class="mb-2"><i class="bi bi-check2 text-success me-2"></i> Beginner to expert content</li>
                            </ul>
                            <div class="mt-auto">
                                <a href="#" class="btn btn-primary">Browse Tutorials</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm overflow-hidden h-100">
                <div class="row g-0 h-100">
                    <div class="col-md-5 bg-success d-flex align-items-center justify-content-center text-white">
                        <div class="p-4 text-center">
                            <i class="bi bi-tools display-1 mb-3"></i>
                            <h3>Tools</h3>
                        </div>
                    </div>
                    <div class="col-md-7">
                        <div class="card-body h-100 d-flex flex-column">
                            <h4 class="card-title">AI Tool Reviews</h4>
                            <p class="card-text">Detailed reviews and comparisons of the latest AI tools to help you find the right ones for your workflow.</p>
                            <ul class="list-unstyled mt-auto">
                                <li class="mb-2"><i class="bi bi-check2 text-success me-2"></i> Honest evaluations</li>
                                <li class="mb-2"><i class="bi bi-check2 text-success me-2"></i> Feature comparisons</li>
                                <li class="mb-2"><i class="bi bi-check2 text-success me-2"></i> Pricing breakdowns</li>
                            </ul>
                            <div class="mt-auto">
                                <a href="#" class="btn btn-success">Compare Tools</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Call to Action Section -->
<div class="container-fluid bg-primary bg-gradient text-white py-5 mb-5">
    <div class="container text-center">
        <h2 class="display-5 fw-bold mb-3">Level Up Your AI Skills</h2>
        <p class="lead fs-4 mb-4">Explore our comprehensive collection of tips, tutorials, and resources to master AI-powered productivity tools.</p>
        <a asp-page="/Tips/Index" class="btn btn-light btn-lg px-4 py-2">Start Learning</a>
    </div>
</div>
